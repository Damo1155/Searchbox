import React from "react";

type SearchBoxType = {
    variant?: "default" | "custom";
}

export const SearchBox = ({
    variant = "default",
}: SearchBoxType) => {
    return (
        <div>TEST</div>
    )
};